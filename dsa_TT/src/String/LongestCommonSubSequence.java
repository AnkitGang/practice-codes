package String;

public class LongestCommonSubSequence {
	private static String minLen(String str1, String str2) {
		return str1.length() > str2.length() ? str1 : str2;
	}
	
	private static void commonSubSequence(String str1, String str2) {
		String smallestStr = minLen(str1, str2);
		if(smallestStr)
		String ans = "";
		for(int i=0; i<smallestStr.length(); i++) {
			if(smallestStr.charAt(i) == )
		}
		
	}
	
	public static void main(String[] args) {
		
	}
}
